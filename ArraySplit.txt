class Solution {
    public boolean isPossibleToSplit(int[] nums) {
      if (nums.length < 2) return false;
     int first=nums[0];
     for(int i=1;i<nums.length;i++){
     if(nums[i]!=first ){
        return true;
    }
 }
         return false;  
}
}